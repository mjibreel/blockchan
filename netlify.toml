[build]
  base = "frontend"
  publish = "build"
  command = "npm install && npm run build"

[build.environment]
  # Ensure environment variables are available during build
  NODE_VERSION = "18"
  CI = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
